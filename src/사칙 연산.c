#include <stdio.h>

int main(void)
{
  int x = 20;
  int y = 10;

  printf("두수의 합 x + y = %d\n", x + y);
  printf("두수의 차 x - y = %d\n", x - y);
  printf("두수의 곱 x * y = %d\n", x * y);
  printf("두수의 몫 x / y = %d\n", x / y);

  return 0;
}
