#include <stdio.h>

int main(void) {
  int x;
  int y;

  int a, b, c, d;

  a = x + y;
  b = x - y;
  c = x * y;
  d = x / y;

  printf("두수의 합 %d", a);
  printf("두수의 차 %d", b);
  printf("두수의 곱 %d", c);
  printf("두수의 몫 %d", d);

  return 0;
}
